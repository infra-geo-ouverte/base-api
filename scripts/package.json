{
  "type": "module",
  "scripts": {
    "pre-release": "node --import tsx ./src/pre-release.mts",
    "publish": "node --import tsx ./src/publish.mts"
  },
  "devDependencies": {
    "execa": "^9.0.0",
    "rimraf": "^6.0.0"
  }
}
